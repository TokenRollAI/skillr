{
  "name": "skillr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:infra": "docker compose -f docker/docker-compose.yml up -d",
    "dev:infra:down": "docker compose -f docker/docker-compose.yml down -v",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "bash tests/e2e/test-full-flow.sh",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "argon2"
    ]
  }
}
